/*Title: Math Tutor V2-S
*Programmers: Cesar
 * Date: 10/4/2025
 * Description: Checks answers and randomizes questions. Also randomizes symbols
 *
 */

#include <iostream>
#include <random>
using namespace std;

int main()
{
    int leftNum = 2;
    int rightNum = 3;
    int userAnswer = 5;
    int correctAnswer = 5;
    char mathSymbol = '+';
    int mathSymbolNumber = 1;
    int swapNum = 0;

    string userName = "unknown";

    //Title of Math Tutor
    cout << "*********************************************************" << endl;
    cout << "    __  __       _   _       _____      _             " << endl;
    cout << "   |  \\/  | __ _| |_| |__   |_   _|   _| |_ ___  _ __ " << endl;
    cout << "   | |\\/| |/ _` | __| '_ \\    | || | | | __/ _ \\| '__|" << endl;
    cout << "   | |  | | (_| | |_| | | |   | || |_| | || (_) | |   " << endl;
    cout << "   |_|  |_|\\__,_|\\__|_| |_|   |_| \\__,_|\\__\\___/|_|2.0  " << endl << endl;
    cout << "*********************************************************" << endl << endl;

    cout << "Fun Math Jokes" << endl;
    cout << "   -Why was 6 afraid of 7? Because 7 8 9! XD" << endl;
    cout << "   -What happens when you divide 0 by anything? Nothing!" << endl;
    cout << "   -Why was pi in trouble with his parents? Because he kept going on and on!" << endl << endl;;

    cout << "What is your name?...";

    getline(cin, userName);

    cout << "Welcome to the Math Tutor! " << userName << endl;

    // VARRIABLES
    random_device rd;
    mt19937 gen(rd());
    uniform_int_distribution<> dist(1, 10);
    leftNum = dist(gen);
    uniform_int_distribution<> dist2(1, 10);
    rightNum = dist2(gen);
    correctAnswer = leftNum + rightNum;

    uniform_int_distribution<> dist3(1, 4);
    mathSymbolNumber = dist3(gen);

    //SYMBOL randomizer
    if (mathSymbolNumber == 1) {
        correctAnswer = leftNum + rightNum;
        cout << "What is " << leftNum << "+" << rightNum << "..." <<  endl;
    }
    else if (mathSymbolNumber == 2) {
        if (rightNum > leftNum)
        {
            swapNum = leftNum;
            leftNum = rightNum;
            rightNum = swapNum;
        }
        correctAnswer = leftNum - rightNum;
    cout << "What is " << leftNum << "-" << rightNum << "..." <<  endl;
    }
    else if (mathSymbolNumber == 3) {
        correctAnswer = leftNum * rightNum;
        cout << "What is " << leftNum << "*" << rightNum << "..." <<  endl;
    }
    else if (mathSymbolNumber == 4)
    {
        if (leftNum%rightNum != 0)
        {
            do {
                /*limitation: does not do same number division
                 *A= its too easy
                 *B= same number division takes up too much answers.
                 */
                cout << "rebooting division..." << endl;
                uniform_int_distribution<> dist4(1, 10);
                leftNum = dist4(gen);
                rightNum = dist4(gen);

            } while (leftNum%rightNum != 0 && leftNum!=rightNum);
        }
        correctAnswer = leftNum / rightNum;
        cout << "What is " << leftNum << "/" << rightNum << "..." <<  endl;
    }
    else
    {
        cout << "ERROR! Mathsym diddnt work. send error code -1 to the following github" << endl;
        cout << "https://github.com/Oktronika" << endl;
    }




    //answer checker and end of program

    cin >> userAnswer;

    if (userAnswer == correctAnswer) {
        cout << "Correct! you inputed..." << correctAnswer << endl;
    } else {
        cout << "Wrong. The answer is..." << correctAnswer <<  endl;
    }
    cout << "______________________________________________________" << endl;
    cout << "PROGRAM TERMINATED" << endl;


    return 0;
}
